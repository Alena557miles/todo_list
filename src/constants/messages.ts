export function minNameLengthMessage(min: number): string {
  return `Input is too short. Minimum: ${min} characters`
}

export function maxNameLengthMessage(max: number): string {
  return `Input is too long. Limit: ${max} characters`
}
